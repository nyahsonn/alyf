fastapi==0.121.2
uvicorn[standard]==0.40.0
sqlalchemy[asyncio]==2.0.44
asyncpg==0.30.0
pgvector==0.4.2
pydantic==2.12.4
pydantic-settings==2.13.0
python-multipart==0.0.20

# Development / testing
# anthropic is used by app/extraction/home_inspection.py (structured, LLM-based
# extraction) and scripts/check_claude_api.py (a connectivity check) — the rest
# of the pipeline runs fully offline and never calls a model.
anthropic==0.120.2
# google-cloud-documentai and pypdf are used by app/ingestion/ocr.py to OCR
# PDF uploads (see README, "PDFs") — the rest of the pipeline stays offline.
# pypdf counts pages locally before a request is sent, since Document AI's own
# enforcement of its page limit is not reliable. google-cloud-storage is only
# needed for PDFs over ONLINE_PAGE_LIMIT pages, which go through Document AI's
# batch API -- that reads its input from Cloud Storage and writes its output
# there too, rather than taking the file directly.
google-cloud-documentai==3.15.0
google-cloud-storage==3.13.1
pypdf==6.15.0
pytest==8.4.2
pytest-asyncio==1.3.0
httpx==0.28.1
